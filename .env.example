# Coolify instances (comma-separated for multi-instance)
# Format: name=url|token
COOLIFY_INSTANCES=production=https://coolify.example.com|your-api-token

# Or single instance (backwards compatible)
COOLIFY_BASE_URL=https://coolify.example.com
COOLIFY_ACCESS_TOKEN=your-api-token
